spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://db.kcrkfpehhvnaokbiawtc.supabase.co:5432/postgres?sslmode=require}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:Chemm@saini}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://kcrkfpehhvnaokbiawtc.supabase.co/auth/v1
          jwk-set-uri: https://kcrkfpehhvnaokbiawtc.supabase.co/auth/v1/jwks

server:
  port: ${PORT:8080}

logging:
  level:
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG
